use std.runtime;
use std.types.string;

use print: std.print;

$main.symbol = "main";
fun main(argc: i64, argv: &&u8) i64 {
    print.println("Mach Language Server");
    print.println("--------------------");
    ret 0;
}
